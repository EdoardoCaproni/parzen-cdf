\documentclass[11pt]{article}

\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{xcolor}

\graphicspath{{../results/}}

\title{Neural Estimation of a CDF and its Density from Logistic Parzen Targets\\
\large Progressive experiment report --- branch \texttt{experiments/cdf-extension}}
\author{parzen-cdf}
\date{\today}

\newcommand{\Fhat}{\widehat{F}}
\newcommand{\fhat}{\widehat{f}}
\newcommand{\KS}{\mathrm{KS}}

\begin{document}
\maketitle

\begin{abstract}
We train a sigmoidal multilayer perceptron (MLP) to approximate the cumulative distribution
function (CDF) of a known Gaussian mixture, using as supervision only the logistic Parzen-window
estimate evaluated at the drawn sample points, and recover the density as the derivative of the
learned CDF. This report grows one experiment at a time: each section states a hypothesis, fixes a
single experimental knob, and scores the result against the analytic truth. It is the LaTeX
counterpart of the Markdown track record in \texttt{docs/experiments.md}.
\end{abstract}

\section{Pipeline and constraints}

The pipeline is, end to end:
\begin{enumerate}
  \item Choose a known mixture of Gaussians, with closed-form pdf $f$ and CDF $F$.
  \item Draw $n$ i.i.d.\ samples $X = \{x_1,\dots,x_n\}$.
  \item Fit a logistic-window Parzen estimator on the samples (unsupervised), with bandwidth $h$:
  \begin{equation}
    \Fhat(x) = \frac{1}{n}\sum_{i=1}^{n}\sigma\!\left(\frac{x-x_i}{h}\right),
    \qquad
    \fhat(x) = \frac{1}{n}\sum_{i=1}^{n}\frac{1}{h}\,\sigma(z_i)\bigl(1-\sigma(z_i)\bigr),
    \label{eq:parzen}
  \end{equation}
  with $\sigma$ the logistic sigmoid and $z_i=(x-x_i)/h$. The logistic kernel is chosen because its
  integral is $\sigma$, giving a closed-form, smooth, strictly increasing CDF estimate; $\fhat$ is
  exactly $\mathrm{d}\Fhat/\mathrm{d}x$.
  \item Train a sigmoidal MLP $F_\theta$ to approximate the \emph{true} CDF, using the label
  $\Fhat(x_i)$ at each sample point $x_i$.
  \item Recover the density as $f_\theta = \mathrm{d}F_\theta/\mathrm{d}x$ (autograd), or via the
  copula (Sklar; multivariate, deferred).
\end{enumerate}

\paragraph{Hard constraints.} (i) \emph{Train on the data points only}: inputs are exactly the $n$
samples $x_i$, labels are $\Fhat(x_i)$; we never sample $\Fhat$ at synthetic $x$, build collocation
grids, or augment the data. (ii) The learned CDF must be \emph{monotone non-decreasing}, enforced by
a loss penalty, a downstream rectification, or a monotone-by-construction architecture. (iii) The
network is sigmoidal and the targets are Gaussian mixtures.

\paragraph{Note on the inherited code.} The original training-set builder samples $\Fhat$ at
$\sim\!10^3$ uniform collocation points, which violates constraint~(i); it is retained only as an
out-of-constraint reference. All experiments below use the constraint-correct builder
$\{(x_i,\Fhat(x_i))\}_{i=1}^n$.

\section{Method and conventions}

\paragraph{Referee.} Because the data is synthetic, every estimate is scored against the analytic
truth on a dense evaluation grid. The grid and the truth are used for evaluation only; they are
never training data.

\paragraph{Metrics.} We report the Kolmogorov--Smirnov distance on the CDF,
$\KS = \max_x |F(x)-F_\theta(x)|$; the mean squared error of the pdf; the monotonicity violation
fraction (the share of adjacent grid steps on which the estimated CDF decreases); and the recovered
pdf mass $\int f_\theta\,\mathrm{d}x$, which should equal $1$. The Parzen estimate of
Eq.~\eqref{eq:parzen} is the target the MLP fits, hence the accuracy \emph{ceiling} for a given $h$.

\section{Experiments}

\subsection{E0 --- Naive baseline (data points only)}

\paragraph{Hypothesis.} A small sigmoidal MLP trained on $\{(x_i,\Fhat(x_i))\}$ recovers the Parzen
target reasonably; monotonicity may not yet be binding; monotone-by-construction (Sill) costs
accuracy.

\paragraph{Setup.} All four mixtures; $n=2000$; fixed Silverman bandwidth; one hidden layer of width
$16$ with sigmoid activations and a final sigmoid; Adam at learning rate $10^{-2}$ for $2000$ epochs;
seed $0$. Three monotonicity arms: \texttt{baseline} (unconstrained), \texttt{soft} (penalty
$\lambda=5$ on $\mathrm{relu}(-\mathrm{d}F_\theta/\mathrm{d}x)$ over the observed data range), and
\texttt{sill} (non-negative weights, monotone by construction). The density is the clamped autograd
derivative.

\begin{table}[h]
  \centering
  \caption{E0 naive baseline. The \texttt{parzen} row is the target/ceiling. Lower $\KS$ and MSE are
  better; pdf mass should be $\approx 1$.}
  \begin{tabular}{llrrrr}
    \toprule
    Distribution & Variant & $\KS$(CDF) & MSE(pdf) & viol.\ \% & pdf mass \\
    \midrule
    \multirow{4}{*}{single\_gaussian ($h{=}0.196$)}
      & parzen   & 0.0196 & 0.00019 & 0.00 & 0.9999 \\
      & baseline & 0.0319 & 0.00024 & 0.00 & 0.9793 \\
      & soft     & 0.0319 & 0.00024 & 0.00 & 0.9793 \\
      & sill     & 0.0774 & 0.00094 & 0.00 & 0.8786 \\
    \midrule
    \multirow{4}{*}{symmetric\_bimodal ($h{=}0.413$)}
      & parzen   & 0.0477 & 0.00154 & 0.00 & 0.9991 \\
      & baseline & 0.1144 & 0.00828 & 0.00 & 0.9092 \\
      & soft     & 0.1144 & 0.00828 & 0.00 & 0.9092 \\
      & sill     & 0.1599 & 0.00916 & 0.00 & 0.8266 \\
    \midrule
    \multirow{4}{*}{asymmetric\_trimodal ($h{=}0.443$)}
      & parzen   & 0.0470 & 0.00232 & 0.00 & 0.9957 \\
      & baseline & 0.0883 & 0.00474 & 0.00 & 0.9096 \\
      & soft     & 0.0883 & 0.00474 & 0.00 & 0.9096 \\
      & sill     & 0.1268 & 0.00545 & 0.00 & 0.8164 \\
    \midrule
    \multirow{4}{*}{spike\_in\_broad ($h{=}0.158$)}
      & parzen   & 0.0542 & 0.00336 & 0.00 & 0.9999 \\
      & baseline & 0.0590 & 0.00361 & 0.00 & 0.9729 \\
      & soft     & 0.0590 & 0.00361 & 0.00 & 0.9729 \\
      & sill     & 0.0934 & 0.00717 & 0.00 & 0.8783 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{e0_naive_asymmetric_trimodal.png}
  \caption{E0 on the asymmetric trimodal mixture. Left: CDF; right: recovered pdf. The width-16 net
  under-fits (single smooth bump), and the Parzen target (gray, dashed) is itself over-smoothed by
  the Silverman bandwidth.}
\end{figure}

\paragraph{Observations.}
\begin{enumerate}
  \item \texttt{baseline} and \texttt{soft} are \emph{identical}: the unconstrained net already has
  zero monotonicity violations, so the penalty $\mathrm{mean}(\mathrm{relu}(-\mathrm{d}F/\mathrm{d}x))$
  is zero and contributes no gradient. Monotonicity is not binding at this scale.
  \item The net under-fits: the recovered pdf is a single smooth mode that misses the multimodal
  structure, although it fits its labels well ($\text{train MSE}\sim 3\times10^{-4}$). This points to
  the capacity and training-length axes.
  \item Two stacked sources of error: even a perfect fit to the Parzen target cannot reach the truth,
  because Silverman over-smooths the target. The bandwidth/target axis must improve alongside the fit.
  \item Sill underperforms on every distribution and loses the most mass ($0.82$--$0.88$): the
  non-negativity constraint costs expressiveness at width $16$.
  \item All neural arms under-integrate ($0.82$--$0.98$) versus Parzen ($\approx 1.0$): the learned
  CDF does not fully span $0\to1$ over the grid.
\end{enumerate}

\paragraph{Verdict.} Baseline established in a clear under-fitting regime with an over-smoothed
target. This was a premature cross-distribution snapshot; it is superseded by the staged dives that
explore each distribution's inner ablation deeply (Sec.~\ref{sec:stageA}).

\subsection{Stage A --- single Gaussian, inner ablation}
\label{sec:stageA}

We now treat distribution complexity as the outer progression and the ablation axes as the inner
deep-dive, beginning with the simplest target $N(0,1)$. Each knob is varied off the naive baseline
(width $16$, lr $10^{-2}$, $2000$ epochs, Silverman $h$, $n=2000$, unconstrained), training on the
data points only, scored on a fixed grid $[-6,6]$. The Parzen ceiling is $\KS=0.0196$.

\begin{table}[h]
  \centering
  \caption{Stage A inner ablation on $N(0,1)$. One knob varied per block; $\KS$ against the truth,
  recovered pdf mass. Bold marks the best in each block; the Parzen ceiling is $\KS=0.0196$.}
  \small
  \begin{tabular}{llr r @{\hskip 2em} llr r}
    \toprule
    \multicolumn{4}{c}{} & \multicolumn{4}{c}{} \\[-1.8ex]
    \multicolumn{2}{l}{E1 capacity} & $\KS$ & mass & \multicolumn{2}{l}{E2 optimisation} & $\KS$ & mass \\
    \midrule
    width & 16          & 0.0319 & 0.993 & lr     & $10^{-2}$ & 0.0319 & 0.993 \\
    width & 64          & 0.0273 & 0.998 & lr     & $3\!\cdot\!10^{-2}$ & 0.0290 & 0.997 \\
    width & 128         & 0.0263 & 0.998 & lr     & $\mathbf{10^{-1}}$ & \textbf{0.0193} & 0.997 \\
    depth & 2 (32,32)   & \textbf{0.0208} & 0.995 & epochs & 5000 & 0.0210 & 0.998 \\
    depth & 3 (32,32,32)& 0.0421 & 0.988 & epochs & \textbf{10000} & \textbf{0.0197} & 0.999 \\
    \midrule
    \multicolumn{2}{l}{E3 bandwidth} & $\KS$ & mass & \multicolumn{2}{l}{E4 / E5} & $\KS$ & mass \\
    \midrule
    scale & $0.3\times$ & 0.0261 & 0.994 & $n$       & 8000     & 0.0249 & 0.993 \\
    scale & $1.0\times$ & 0.0319 & 0.993 & $n$       & 2000     & 0.0319 & 0.993 \\
    scale & $1.5\times$ & 0.0409 & 0.991 & soft     & any $\lambda$ & 0.0319 & 0.993 \\
    adaptive &          & \textbf{0.0256} & 0.993 & sill     & ---      & 0.0774 & 0.884 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{stage_a_single_gaussian.png}
  \caption{Stage A inner ablation on $N(0,1)$: $\KS$ versus each knob. Gray dashed line is the Parzen
  target ceiling.}
\end{figure}

\paragraph{Findings.} (i) The naive baseline is \emph{under-trained}; optimisation is the dominant
lever --- $\mathrm{lr}=0.1$ or $10^4$ epochs each reach the Parzen ceiling. (ii) Capacity helps with
diminishing returns; a depth-2 net is best, while depth-3 is harder to train at the same budget.
(iii) For a unimodal target the bandwidth is a minor lever (slightly finer / adaptive is best,
over-smoothing worst); its effect should grow on multimodal targets. (iv) Sample count gives a
modest, noisy gain. (v) Monotonicity is not binding (the soft penalty is inactive at every
$\lambda$); Sill is a pure accuracy/mass cost. The unconstrained sigmoidal MLP can match its Parzen
target on $N(0,1)$; whether this survives a multimodal target is the Stage~B question.

\subsection{Stage B --- simple mixtures, inner ablation}
\label{sec:stageB}

We repeat the inner ablation on two simple bimodals --- $M_1 = 0.5\,N(-2,0.7)+0.5\,N(2,0.7)$ and
$M_2 = 0.65\,N(0,1)+0.35\,N(3,0.6)$ --- at the improved baseline carried from Stage~A (width $32$,
$\mathrm{lr}=0.1$, $5000$ epochs), with the sample-count axis averaged over three seeds.

\begin{table}[h]
  \centering
  \caption{Stage B, E3 bandwidth axis: $\KS$ against the truth as the bandwidth varies. Truth-free
  CV / adaptive selection roughly halves the error versus Silverman ($1.0\times$). Parzen ceilings at
  Silverman: $M_1$ $0.0477$, $M_2$ $0.0270$.}
  \begin{tabular}{lrr}
    \toprule
    bandwidth & $M_1$ $\KS$ & $M_2$ $\KS$ \\
    \midrule
    $0.3\times$ Silverman & 0.0199 & 0.0115 \\
    $1.0\times$ (Silverman) & 0.0495 & 0.0267 \\
    $1.5\times$ (over-smoothed) & 0.0687 & 0.0453 \\
    variance-matched & 0.0258 & 0.0157 \\
    likelihood-CV & 0.0195 & 0.0121 \\
    LSCV & 0.0195 & 0.0125 \\
    \textbf{adaptive (Abramson)} & \textbf{0.0185} & \textbf{0.0121} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.49\textwidth]{stage_b_symmetric_bimodal.png}\hfill
  \includegraphics[width=0.49\textwidth]{stage_b_asymmetric_bimodal.png}
  \caption{Stage B inner ablation: $M_1$ (left), $M_2$ (right). The bandwidth panel (bottom-left)
  shows the dominant downward trend toward finer $h$; capacity and monotonicity panels are flat. The
  $\mathrm{width}=128$ run diverged at $\mathrm{lr}=0.1$ (annotated, excluded from the line).}
\end{figure}

\paragraph{Findings.} (i) \textbf{Bandwidth is the dominant lever} on mixtures: CV / adaptive
selectors halve $\KS$ versus Silverman, and the gain is truth-free. (ii) The network is a
\emph{faithful regressor} --- its $\KS$ tracks the Parzen $\KS$ across both $n$ and $h$, so the
accuracy bottleneck is the target, not the net. (iii) Capacity plateaus early (width $\geq 8$
suffices); the Stage~A under-fit was an optimisation, not a capacity, issue. (iv) $\mathrm{lr}=0.1$
diverges at width $128$ (KS $1.0$, mass $0$): the safe rate is $\approx 0.03$. (v) Monotonicity is
still not binding (zero violations; the soft penalty inactive; Sill now competitive). Net: through
simple mixtures the regressor saturates its target, and the highest-leverage knob within the
constraints is truth-free bandwidth selection.

\section{Planned experiments}

\begin{itemize}
  \item \textbf{E1 Capacity:} hidden width and depth.
  \item \textbf{E2 Optimization:} learning rate and epochs.
  \item \textbf{E3 Bandwidth / target quality:} Silverman $\to$ variance-matched $\to$
  cross-validation $\to$ adaptive (variable window).
  \item \textbf{E4 Sample count:} $n$.
  \item \textbf{E5 Monotonicity strategy:} soft-$\lambda$ vs Sill vs downstream rectification; pdf
  mass renormalisation.
  \item \textbf{E6} Interactions and best-of-axes pipeline; \textbf{E7} pdf-recovery refinements;
  later, multivariate / copula approaches.
\end{itemize}

\end{document}
