# Local-only working notes (not part of the deliverable)
CLAUDE.md

# Virtual environments
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Editable install / packaging
src/*.egg-info/

# Jupyter
.ipynb_checkpoints/

# Testing / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Generated data and experiment artifacts (figures live in the tracked results/ dir)
data/
runs/
*.pt
*.ckpt

# LaTeX build artifacts. The delivered PDF is tracked: it is the thing people read, and
# asking for a LaTeX distribution just to open it defeats the point.
report/*.aux
report/*.log
report/*.out
report/*.toc
report/*.pdf
!report/report3.pdf
report/build.log

# OS / editor
.DS_Store
.vscode/
.idea/
app/checkpoints/
nohup.out
